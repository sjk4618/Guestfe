## 0) 기본 원칙 (필수)
- **모든 응답과 커뮤니케이션은 한국어로 작성한다.**
- **보안을 최우선으로 한다.** 데이터 유출/노출/오남용 가능성이 있으면 작업을 멈추고 위험을 먼저 알린다.
- 기능을 추가/수정하면 **반드시 단위 테스트(Unit Test)를 작성하고 전체 테스트를 실행**한다.
- 보안적으로나 코드 품질 측면에서 이슈가 될 수 있는 부분은 **미리 경고하고, 안전한 대안**을 제시한다.

---

## 1) 보안 가드레일 (절대 준수)
### 1.1 비밀정보(Secrets) 취급
- 아래 정보는 **절대 로그/출력/커밋/공유 금지**:
  - API Key, Access Token, Refresh Token, Password, Private Key, DB 접속 문자열, 쿠키/세션, 인증서, 개인식별정보(PII)
- `.env`, `secrets.*`, 키 파일, 클라우드 크리덴셜 등은 **레포에 커밋하지 않는다.**
- 예시 데이터가 필요하면 **가짜 값(마스킹된 값)** 으로만 작성한다.
- 디버깅/에러 메시지에 비밀정보가 섞일 가능성이 있으면 출력 전에 **마스킹 처리**한다.

### 1.2 로그/에러 처리
- 에러 로그에 요청 바디/헤더/토큰이 그대로 찍히지 않도록 한다.
- 민감 데이터는 `***` 처리하거나, 식별 불가능한 형태로 축약한다.
- 운영환경에서는 **stack trace 전체 노출을 제한**한다(필요 시 내부 로깅에만).

### 1.3 입력 검증 & 인젝션 방지
- 모든 외부 입력(HTTP 파라미터/바디/헤더/파일명/쿼리)은 **검증/정규화**한다.
- SQL/NoSQL/Command/Template/Path Injection을 항상 의심하고 방어한다.
- ORM/Prepared Statement 사용을 우선하며, 동적 쿼리 작성 시 화이트리스트 기반으로 제한한다.

### 1.4 인증/인가
- “인증됨”과 “권한 있음”을 구분한다.
- 리소스 접근은 **최소 권한(Least Privilege)** 원칙으로 제한한다.
- 관리자/특권 기능은 별도 권한 체크를 두고, 실패 시 안전하게 거부한다.

### 1.5 취약한 설계/구현 금지
- 하드코딩된 비밀정보, 약한 암호화/해시(MD5/SHA1 단독), 무작위성 부족, 취약한 CORS 설정, SSRF 가능 URL fetch 등을 금지한다.
- 파일 업로드/다운로드 기능은 MIME/확장자/크기/저장경로/권한을 엄격히 제한한다.
- 외부 URL 요청이 필요하면:
  - 내부망 접근 차단
  - 허용 도메인 화이트리스트
  - 타임아웃/리다이렉트 제한
  - 응답 크기 제한
  - DNS Rebinding/SSRF 방어
  를 고려한다.

---

## 2) 개발 프로세스 규칙
### 2.1 작업 전
- 요구사항/변경 범위를 짧게 정리하고, **보안 영향(Threat/Abuse case)을 1~3개라도 먼저 점검**한다.
- 기존 코드 스타일/아키텍처를 우선 존중한다(불필요한 리팩토링 금지).

### 2.2 구현
- 작은 단위로 커밋 가능한 변경을 목표로 한다.
- 공개 API/DTO 변경은 호환성을 고려하고, Breaking change가 있으면 명확히 표시한다.
- 의심되는 보안/품질 리스크가 있으면 **구현 단계에서 즉시 언급**하고 해결책을 적용한다.

### 2.3 테스트 (필수)
- 기능 추가/수정 시 **단위 테스트를 반드시 추가**한다.
- 테스트는 “정상 케이스 + 경계값 + 실패/예외 케이스”를 포함한다.
- 보안 관련 기능(인가, 입력 검증, 마스킹)은 **테스트로 회귀 방지**한다.
- 가능하면 `CI`에서 테스트가 자동 실행되도록 유지한다.

### 2.4 완료 조건 (Definition of Done)
- [ ] 한국어로 변경 내용/사용 방법/주의사항을 설명했다.
- [ ] 단위 테스트가 추가되었고, 전체 테스트가 통과했다.
- [ ] 민감정보 노출/로그 유출 가능성이 없는지 점검했다.
- [ ] 잠재적 취약점/품질 이슈를 사전에 공유했고, 대응책을 반영했다.

---

## 3) 코드 품질 규칙
- 가독성을 우선: 명확한 네이밍, 작은 함수, 단일 책임.
- 예외 메시지는 사용자에게 과도한 내부 정보를 보여주지 않는다.
- 공용 유틸/헬퍼는 범용성보다 **안전성**을 우선한다.
- 라이브러리/의존성 추가는 최소화하고, 추가 시 보안/라이선스 리스크를 고려한다.

---

## 4) 문서화 규칙
- 변경사항이 사용자/운영에 영향이 있으면 README 또는 관련 문서에 반영한다.
- 보안상 민감한 설정/키/내부 URL은 문서에 그대로 적지 않는다(플레이스홀더 사용).

---

## 5) 금지 사항
- 실제 토큰/키/계정정보를 생성/노출/전송하는 행위
- 사용자 데이터 덤프, 내부 DB 스키마/데이터를 외부로 그대로 복사
- “임시로” 넣었다며 비밀값을 코드에 하드코딩하는 행위
- 테스트 없이 기능을 완료 처리하는 행위
